# PATCHES

- **Kconfig**
for ~/.env/packages/packages/tools/blackmagic/Kconfig

- **package.json**
for ~/.env/packages/packages/tools/blackmagic/package.json

- **env-package.patch**
patch to "env" package directory to add blackmagic as a new package

